#include <string>
#include <list>
#include "tutor.h"
using namespace std;

enum class Sex{M,F};
enum class Subject{
    CHINESE,
    ENGLISH,
    MATHS,
    M1,
    M2,
    LS,
    PHYS,
    CHEM,
    BIO,
    GEOG,
    C_HISTORY,
    W_HISTORY,
    ICT,
    ECON,
    BAFS,
    ACCOUNT,
    IS,
    CHINLIT,
    ENGLIT,
    VA
};
enum class MainLocation{
    NT,
    KL,
    HKI
};
enum class NT{
    TIN_SHUI_WAI,
    YUEN_LONG,
    TUEN_MUN
};
enum class KL{

};
enum class HKI{};

class Matching{
    private:
        

    public:
        Matching();        
};