#include "student.h"

Student::Student(string number,
        string name,
        string sex,
        string grade,
        string subjects,
        string locations,
        string fee):
            number(number),
            name(name),
            sex(sex),
            grade(grade),
            subjects(subjects),
            locations(locations),
            fee(fee){}